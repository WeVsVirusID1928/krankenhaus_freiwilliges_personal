export interface ChipValue {
    key: string;
    label: string;
}

export interface HelpingHand {
    firstName: string;
    lastName: string;
    zipCode: string;
    maxDistance: number;
    skillSet: [];
}